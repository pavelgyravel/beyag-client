require 'spec_helper'

describe EripClient do
  it 'has a version number' do
    expect(EripClient::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
